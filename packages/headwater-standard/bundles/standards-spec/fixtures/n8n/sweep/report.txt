A coherence sweep over .agents/review-rules, 7 of the 7 classified documents of this corpus.
Every finding below was written by an agent. Provenance: agent. Nothing here is a verdict, and no exit status reads it.

.agents/review-rules/README.md:37:1 · info
  sweep.quiet_supersession: The Layout table above names a `db-migrations/` agent and maps the shared coverage rule to Backend and Frontend alone, so three agents decline it, and the prose two paragraphs below still names two and gives a reason that does not cover a migration.
  fix: read both passages. Declare `supersedes` from the newer document, or correct the older one
  quoted: "| `testing/` | Backend + Frontend | any package with a test …" at .agents/review-rules/README.md:37
  quoted: "Security and QA & DX deliberately don't link `testing/`: cov…" at .agents/review-rules/README.md:41

.agents/review-rules/README.md:39:1 · info
  sweep.undeclared_conflict: The README rules that a policy identical across domains is one shared file and never a copy per directory, and a policy that flags untested new behavior is stated once in the shared file and again in one directory's own file.
  fix: read both passages. Declare `conflicts_with` between the two documents, or record why they do not conflict
  quoted: "A directory maps to one agent unless, like `testing/`, the p…" at .agents/review-rules/README.md:39
  quoted: "Flag new behaviour that ships with no test" at .agents/review-rules/testing/coverage.md:21
  quoted: "A data migration needs an integration test. Reading cannot v…" at .agents/review-rules/db-migrations/conventions-and-tests.md:40
  compared: .agents/review-rules/README.md, .agents/review-rules/testing/coverage.md, .agents/review-rules/db-migrations/conventions-and-tests.md
  proposes: N8N-STD-coverage conflicts_with N8N-STD-conventions-and-tests, which the graph does not declare today
  to declare it, in the front matter of .agents/review-rules/README.md:
      relations:
        conflicts_with:
          - N8N-STD-conventions-and-tests

.agents/review-rules/README.md:70:1 · info
  sweep.undefined_concept: The first instruction to an author of a new rule is to pick a level of reach, and no document of this slice says what the levels are or how to tell them apart.
  fix: define the term where the corpus defines its terms, or narrow the documents that use it
  quoted: "Pick the level of reach first." at .agents/review-rules/README.md:70

3 carried, 0 refused, of 3 the file held. The engine confirmed the citation and the novelty of every carried finding, and it confirmed nothing about whether the claim is true.
One carried finding proposes an edge, which is the outcome spec 4 asks a sweep for. Nothing here writes one: a person accepts a proposal, and the engine never accepts its own.
