---
doc_type: design_spec
id: SPEC-FIX-quotes-marker
---

# A document that quotes the marker

The first line of this file is `---`, so this is an authored document. It names
`headwater:generated` here, below the first line, because a specification has to
be able to discuss the marker without becoming one. A rule that searched the
whole file would classify this document as generated and never check it again.
