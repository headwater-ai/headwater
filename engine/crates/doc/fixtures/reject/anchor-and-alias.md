---
defaults: &defaults
  warrant: accepted
provenance: *defaults
---

The Q2 reuse rulings reach a document's front matter for the reason they were
made, and `check --fix` cannot rewrite one facet of an aliased block.
