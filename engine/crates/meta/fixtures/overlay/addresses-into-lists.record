12:3: add.regimes.lifecycle.standard.transitions.draft.0: `regimes.lifecycle.standard.transitions.draft.0` addresses a position inside the list at `regimes.lifecycle.standard.transitions.draft`; a list position does not survive an upstream release, and `add_to` and `remove_from` are what a list takes instead
13:3: add.core.requires.0: `core.requires.0` addresses a position inside the list at `core.requires`; a list position does not survive an upstream release, and `add_to` and `remove_from` are what a list takes instead
14:3: add.projections.0: `projections.0` addresses a position inside the list at `projections`; a list position does not survive an upstream release, and `add_to` and `remove_from` are what a list takes instead
15:3: add.kinds.decision.sections.require.1: `kinds.decision.sections.require.1` addresses a position inside the list at `kinds.decision.sections.require`; a list position does not survive an upstream release, and `add_to` and `remove_from` are what a list takes instead
19:3: add_to.shelves.decisions.path: `shelves.decisions.path` does not name a list, and `add_to` and `remove_from` are what a list takes; every other node takes `add`, `override` or `remove`
