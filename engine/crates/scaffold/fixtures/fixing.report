fixed check/evaluations/beta.md 1
fixed check/spec/00-both-halves.md 1
fixed check/spec/02-cited-only.md 1
fixed check/spec/07-prose-defects.md 4

findings before
  check/evaluations/delta.md relation.reciprocity.missing `EVAL-FIX-delta` declares `cited_by: SPEC-FIX-cited-only`, and `cites_evidence` requires both ends, so check/spec/02-cited-only.md owes `cites_evidence`
  check/evaluations/epsilon.md relation.participation.overdue `evidence-cited`: 221 days since `status_since`, and this `evaluation` reaches no `cites_evidence` to a `design_spec` inside the 30 days the taxonomy allows
  check/evaluations/eta.md relation.endpoint.not_permitted `assesses` declares `from: [design_spec]`, and `EVAL-FIX-eta` at that end has the kind `evaluation`
  check/evaluations/gamma.md shelf.placement_is_primary `evaluations` is homogeneous and carries the kind `evaluation`, and this document restates it as `doc_type: evaluation`
  check/evaluations/gamma.md relation.reciprocity.missing `EVAL-FIX-gamma` declares `cited_by: SPEC-FIX-both-halves`, and `cites_evidence` requires both ends, so check/spec/00-both-halves.md owes `cites_evidence`
  check/spec/01-one-half.md relation.reciprocity.missing `SPEC-FIX-one-half` declares `cites_evidence: EVAL-FIX-beta`, and `cites_evidence` requires both ends, so check/evaluations/beta.md owes `cited_by`
  check/spec/01-one-half.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 34
  check/spec/03-no-instance.md coverage.document_unchecked this document is classified and all 2 of its check instances were skipped
  check/spec/05-no-summary.md facet.required.missing `design_spec` requires the facet `summary`, and it is not declared
  check/spec/05-no-summary.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 31
  check/spec/06-retired.md facet.value.not_permitted `status` admits draft, current, superseded, and this document declares `retired`
  check/spec/06-retired.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 34
  check/spec/07-prose-defects.md voice.forbidden_construction `declarative` forbids future_intent, and this sentence writes `will be`
  check/spec/07-prose-defects.md voice.forbidden_construction `declarative` forbids change_narration, and this sentence writes `we renamed`
  check/spec/07-prose-defects.md voice.forbidden_construction `declarative` forbids phased_rollout, and this sentence writes `for now`
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 43
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` admits no contraction, and this sentence writes `doesn't`
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` declares the tag `en-US`, and this sentence writes `behaviour`
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` admits no semicolon in running prose, and this sentence writes one
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` admits no contraction, and this sentence writes `doesn't`
  check/spec/07-prose-defects.md language.retired_term.used `ste_house` retires `reference system`, and this sentence writes it: the corpus renamed it
  check/spec/07-prose-defects.md language.retired_term.used `ste_house` retires `robust`, and this sentence writes it: it stands in for the failure the thing survives
  check/spec/07-prose-defects.md language.source_form.not_met `ste_house` writes one paragraph on one line, and this line continues the paragraph above
  check/spec/07-prose-defects.md language.source_form.not_met `ste_house` writes one paragraph on one line, and this line continues the paragraph above
  check/spec/07-prose-defects.md link.fragment.unresolved `#no-such-section` names no heading of this document
  check/spec/09-contract-missing.md section.required.missing `decision_record` requires the section `Decision`, and no heading of this document says so
  check/spec/10-suppressed.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 31
  check/spec/11-identifier-mismatch.md identifier.pattern.not_met `SPEC-HW-mismatch` does not match `SPEC-FIX-<slug>`, which scheme `spec_id` declares: `HW-mismatch` is where the namespace `FIX` was expected
  check/spec/13-dangling.md relation.target.unresolved `SPEC-FIX-dangling` declares `assesses: SPEC-FIX-no-such-document`, and that target resolves to nothing at all
  check/spec/13-dangling.md relation.target.unresolved `SPEC-FIX-dangling` declares `assesses: SPEC-FIX-untyped`, and that target names check/spec/04-untyped.md, which is untyped: the census gave it no kind, and an endpoint is a kind
  check/spec/14-relations-not-a-mapping.md relation.declaration.unusable `relations` is a sequence, and it names relation types
  check/spec/15-unusable-entries.md relation.declaration.unusable `invented_relation` is neither a declared relation nor a declared inverse
  check/spec/15-unusable-entries.md relation.declaration.unusable `assesses` names EVAL-FIX-alpha twice, and the second declares no second edge
  check/spec/15-unusable-entries.md relation.declaration.unusable an entry of `assesses` is a sequence, and an entry is a target or a mapping with `to`
  check/spec/15-unusable-entries.md relation.declaration.unusable an entry of `assesses` declares no `to`, so it names no target
  check/spec/16-no-identifier.md identifier.unusable a typed `design_spec` that declares no identifier, so no edge can name it, and every entry of its `relations:` block is lost with it, because an edge is identified by its source
  check/spec/17-identifier-not-a-word.md identifier.unusable the identifier is not a scalar, and an identifier is a word
  engine/crates/check/fixtures/check.taxonomy.yml control.mechanism.unimplemented control CT-FIX-15 names the mechanism check:no.such.rule, which this engine does not implement, so nothing discharges OB-FIX-18
  engine/crates/check/fixtures/check.taxonomy.yml control.mechanism.unimplemented control CT-FIX-17 names the mechanism phase:no.such.phase, which this engine does not implement, so nothing discharges OB-FIX-20
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-16 carries no disposition: no control discharges it, and it states neither a gap nor an acceptance
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-17 carries two dispositions: CT-FIX-14 discharges it, and it also states one for itself
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-18 carries no disposition: CT-FIX-15 names a mechanism this engine does not implement, so nothing discharges it, and it states neither a gap nor an acceptance
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-20 carries two dispositions: CT-FIX-17 claims to discharge it, and it also states one for itself

findings after
  check/evaluations/epsilon.md relation.participation.overdue `evidence-cited`: 221 days since `status_since`, and this `evaluation` reaches no `cites_evidence` to a `design_spec` inside the 30 days the taxonomy allows
  check/evaluations/eta.md relation.endpoint.not_permitted `assesses` declares `from: [design_spec]`, and `EVAL-FIX-eta` at that end has the kind `evaluation`
  check/evaluations/gamma.md shelf.placement_is_primary `evaluations` is homogeneous and carries the kind `evaluation`, and this document restates it as `doc_type: evaluation`
  check/spec/01-one-half.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 34
  check/spec/03-no-instance.md coverage.document_unchecked this document is classified and all 2 of its check instances were skipped
  check/spec/05-no-summary.md facet.required.missing `design_spec` requires the facet `summary`, and it is not declared
  check/spec/05-no-summary.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 31
  check/spec/06-retired.md facet.value.not_permitted `status` admits draft, current, superseded, and this document declares `retired`
  check/spec/06-retired.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 34
  check/spec/07-prose-defects.md voice.forbidden_construction `declarative` forbids future_intent, and this sentence writes `will be`
  check/spec/07-prose-defects.md voice.forbidden_construction `declarative` forbids change_narration, and this sentence writes `we renamed`
  check/spec/07-prose-defects.md voice.forbidden_construction `declarative` forbids phased_rollout, and this sentence writes `for now`
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 43
  check/spec/07-prose-defects.md language.controlled.not_met `ste_house` admits no semicolon in running prose, and this sentence writes one
  check/spec/07-prose-defects.md language.retired_term.used `ste_house` retires `robust`, and this sentence writes it: it stands in for the failure the thing survives
  check/spec/07-prose-defects.md language.source_form.not_met `ste_house` writes one paragraph on one line, and this line continues the paragraph above
  check/spec/07-prose-defects.md language.source_form.not_met `ste_house` writes one paragraph on one line, and this line continues the paragraph above
  check/spec/07-prose-defects.md link.fragment.unresolved `#no-such-section` names no heading of this document
  check/spec/09-contract-missing.md section.required.missing `decision_record` requires the section `Decision`, and no heading of this document says so
  check/spec/10-suppressed.md language.controlled.not_met `ste_house` holds prose to 25 words a sentence, and this one has 31
  check/spec/11-identifier-mismatch.md identifier.pattern.not_met `SPEC-HW-mismatch` does not match `SPEC-FIX-<slug>`, which scheme `spec_id` declares: `HW-mismatch` is where the namespace `FIX` was expected
  check/spec/13-dangling.md relation.target.unresolved `SPEC-FIX-dangling` declares `assesses: SPEC-FIX-no-such-document`, and that target resolves to nothing at all
  check/spec/13-dangling.md relation.target.unresolved `SPEC-FIX-dangling` declares `assesses: SPEC-FIX-untyped`, and that target names check/spec/04-untyped.md, which is untyped: the census gave it no kind, and an endpoint is a kind
  check/spec/14-relations-not-a-mapping.md relation.declaration.unusable `relations` is a sequence, and it names relation types
  check/spec/15-unusable-entries.md relation.declaration.unusable `invented_relation` is neither a declared relation nor a declared inverse
  check/spec/15-unusable-entries.md relation.declaration.unusable `assesses` names EVAL-FIX-alpha twice, and the second declares no second edge
  check/spec/15-unusable-entries.md relation.declaration.unusable an entry of `assesses` is a sequence, and an entry is a target or a mapping with `to`
  check/spec/15-unusable-entries.md relation.declaration.unusable an entry of `assesses` declares no `to`, so it names no target
  check/spec/16-no-identifier.md identifier.unusable a typed `design_spec` that declares no identifier, so no edge can name it, and every entry of its `relations:` block is lost with it, because an edge is identified by its source
  check/spec/17-identifier-not-a-word.md identifier.unusable the identifier is not a scalar, and an identifier is a word
  engine/crates/check/fixtures/check.taxonomy.yml control.mechanism.unimplemented control CT-FIX-15 names the mechanism check:no.such.rule, which this engine does not implement, so nothing discharges OB-FIX-18
  engine/crates/check/fixtures/check.taxonomy.yml control.mechanism.unimplemented control CT-FIX-17 names the mechanism phase:no.such.phase, which this engine does not implement, so nothing discharges OB-FIX-20
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-16 carries no disposition: no control discharges it, and it states neither a gap nor an acceptance
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-17 carries two dispositions: CT-FIX-14 discharges it, and it also states one for itself
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-18 carries no disposition: CT-FIX-15 names a mechanism this engine does not implement, so nothing discharges it, and it states neither a gap nor an acceptance
  engine/crates/check/fixtures/check.taxonomy.yml obligation.disposition.not_one obligation OB-FIX-20 carries two dispositions: CT-FIX-17 claims to discharge it, and it also states one for itself
