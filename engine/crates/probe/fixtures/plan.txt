A regression probe run over 5 of the 6 classified documents of this corpus.

## The run identity this plan fixes

```yaml
lock: sha256:fixture
tree: sha256:332a25239ab6c0ed50e9063265c7b7b746dcccce12b43acbe8639d51f5ee2c62
selection: sha256:acb39c7d95c364df7aa7c4fd5a084010f3fbe9aacbf0586dc93126281a77084e
read_set: sha256:9cd0571c32a18081b2fd2f031a69d16f7260bd9a9c77e157884ec0d5ea6968fb
seed: 0
harness: 0.1.1
tier: regression
arms: [present]
```

The model, its served version, the wall-clock time and the realized cost belong to the run. The recorder writes those four into the transcript, and `headwater probe record` holds the rest of the identity to the six above.

## The cost

5 probes x 1 arms x 1 repetitions = 5 sessions, at a declared $0.25 each: $1.25 against a ceiling of $20.00.
The unit cost is a number a person declared in `.headwater/probe.yml`. No run has happened, so no realized cost has been recorded against it and this is arithmetic rather than a forecast.

## The selection

- PROBE-FIX-answered (corpus/probes/0002-answered.md)
    category: sufficiency
    expects: answered
    answers: no, yes
- PROBE-FIX-cited (corpus/probes/0004-cited.md)
    category: sufficiency
    expects: cited
    examines: PROBE-FIX-answered at corpus/probes/0002-answered.md
- PROBE-FIX-not-opened (corpus/probes/0003-not-opened.md)
    category: navigability
    expects: not_opened
    examines: PROBE-FIX-opened at corpus/probes/0001-opened.md
- PROBE-FIX-opened (corpus/probes/0001-opened.md)
    category: discovery
    expects: opened
    examines: PROBE-FIX-answered at corpus/probes/0002-answered.md
- PROBE-FIX-patched (corpus/probes/0005-patched.md)
    category: sufficiency
    expects: patched
    oracle: section.required.missing

## The read set

5 documents the `read_set` digest above covers, which is every probe of the selection and every document one of them examines:

- corpus/probes/0001-opened.md (probe) sha256:d096faf5e06e2dc870c3bf0fdb47fa2dd9c8c7883fd1f81d8fabaff7b36ccd9a
- corpus/probes/0002-answered.md (probe) sha256:ab543ee59d5a46fd2fa8f4e1ce81e2de3a68926e0cdb53eacacf2a8b3a4adf76
- corpus/probes/0003-not-opened.md (probe) sha256:37cd08fc8245b8761ba676d7b5c8e7f400c114cfe6f2a2a84c46e5ac4bb7b569
- corpus/probes/0004-cited.md (probe) sha256:331da3480aa9db35b12bd8987fc5cfcda0068010c4969fffbb064f4ac90b347e
- corpus/probes/0005-patched.md (probe) sha256:7913d3261207265c4c9c430a4c89edf0746f3a610abee8230680c5057031454d

A recorded result is stale when a document of this list moves, and it is not stale when any other document of this corpus moves. That is the whole of what the digest buys over the `tree` digest beside it, which moves on every commit.

## What the recorder writes back

One `probe_transcript` document on `docs/probe-runs/`, then `headwater probe record <path>`. The transcript is the ordered tool-call events of each session, and it holds no model prose: the intake refuses a key outside the closed set, which is how the omission is enforced rather than asked for.

**A transcript that an agent wrote about its own session is a self-report, and spec 5 refuses one.** The events have to be observed from outside the session by the process that drove it. That process reaches the network and it is not in this repository, so no verb here writes a transcript and there is no flag that makes one.
