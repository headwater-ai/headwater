shelves.decisions.path => shelves / decisions / path
identifier_schemes.decision_id.pattern => identifier_schemes / decision_id / pattern
vocabularies.lifecycle_state => vocabularies / lifecycle_state
kinds.design_spec.identifier => kinds / design_spec / identifier
kinds.decision_register.language => kinds / decision_register / language
regimes.language.ste_house => regimes / language / ste_house
regimes.voice.narrative => regimes / voice / narrative
relations.forbids => relations / forbids
shelves.playbooks => shelves / playbooks
kinds.playbook => kinds / playbook
shelves => shelves
kinds.adr_2 => kinds / adr_2
kinds.ADR => kinds / ADR
regimes.lifecycle.standard.transitions.0 => regimes / lifecycle / standard / transitions / 0
~ => error at 0: there is nothing to read; an address names at least one segment, and a reference names a root and at least one segment in it
.kinds => error at 0: an empty segment; `.` separates two segments and never opens or closes an address
kinds. => error at 6: an empty segment; `.` separates two segments and never opens or closes an address
kinds..playbook => error at 6: an empty segment; `.` separates two segments and never opens or closes an address
kinds.play-book => error at 10: the character `-` is not allowed in a segment; a segment holds letters, digits and `_`
kinds.play book => error at 10: the character ` ` is not allowed in a segment; a segment holds letters, digits and `_`
$vocabularies.lifecycle_state => error at 0: the character `$` is not allowed in a segment; a segment holds letters, digits and `_`
