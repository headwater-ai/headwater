---
id: NOTE-FIX-ruling-excluded
evidence_basis: asserted
summary: a document at a value that no narrowing on its own kind excludes
---

# The ruling at a basis its chain excludes

Its front matter is the front matter of `opens/any.md`, down to the value.
Only the shelf differs, and the shelf is what says which kind.

`ruling` declares no narrowing at all. `record` above it names `measured` and
`cited`, so the set this document is read against comes from walking the chain
and from nowhere else.
