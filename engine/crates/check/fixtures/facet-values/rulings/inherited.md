---
id: NOTE-FIX-ruling-inherited
evidence_basis: cited
summary: a document of a kind that narrows nothing and inherits a narrowing
---

# The ruling that inherits its narrowing

`ruling` declares no narrowing of its own. It admits what `record` admits, so
`observed` on this document would be a finding and `cited` is not one.
