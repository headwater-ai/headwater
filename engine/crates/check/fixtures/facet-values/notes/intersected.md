---
id: NOTE-FIX-note-intersected
evidence_basis: cited
summary: a document at the one value two narrowings both admit
---

# The note at the intersection

`cited` is the one value that `record` and `note` both name. A rule that read
only the kind's own narrowing would admit `asserted` here as well, and a rule
that read only the parent's would admit `measured`.
