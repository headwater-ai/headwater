---
id: NOTE-FIX-note-excluded
evidence_basis: measured
summary: a document whose kind admits the value it declares
---

# The note at a basis its kind excludes

`measured` is a value of `evidence_basis`, and `record` above this kind admits
it. `note` narrows to `cited` and `asserted`, so the intersection down the
chain holds one value and `measured` is not it.
