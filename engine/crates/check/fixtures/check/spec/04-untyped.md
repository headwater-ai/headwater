---
id: SPEC-FIX-untyped
---

# Untyped

No `doc_type` on a heterogeneous shelf, so the census gives it no kind. It gets
no instance of anything and it is **not** an OB-COV-2 finding: the census
already reports it, with the step that stopped, and a second report of one fact
sends its author to two places.
