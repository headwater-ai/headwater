<!-- headwater:generated shelf_index. `headwater generate` writes this file, and `headwater generate --check` holds it. Edit the corpus, not this file. -->

# An index this engine wrote

This file sits on `spec_series`, which is heterogeneous and picks a kind from a
`doc_type` discriminator. It declares no discriminator, no facet, no identifier
and no section that a contract names. Every one of those is a finding on an
authored document of this shelf, and `04-untyped.md` is that document.

The census reports this row as generated, so no instance is created over it and
no rule reports anything against it. What holds it instead is
`headwater generate --check`.
