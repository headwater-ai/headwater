---
id: SPEC-FIX-voice-facet-prose
doc_type: design_spec
status: current
status_since: 2026-01-05
title: The reference will be rewritten once the lock format settles.
summary: The reference will be rewritten once the lock format settles.
---

# Voice facet prose

The failing fixture for the front-matter arm of `voice.forbidden_construction`. The `summary` line carries a `future_intent` construction, `will be`. The `title` line carries the same phrase and reports nothing. The population is the facet in the `scent` role, and a label in the `name` role is not a sentence.

The body below carries the same construction once, in a sentence of its own. This fixture proves the facet and the body are both read.

The reference will be rewritten once the lock format settles.
