---
id: SPEC-FIX-both-halves
doc_type: design_spec
relations:
  cites_evidence:
    - EVAL-FIX-alpha
---

# Both halves

`alpha.md` writes `cited_by: SPEC-FIX-both-halves`, so this pair is complete and
the reciprocity instance over it passes. It is the passing fixture that spec 12
requires beside a failing one.
