---
id: DR-FIX-0008
doc_type: decision_record
status: current
status_since: 2026-01-05
summary: The passing fixture for the section contract, and the skipping one for two regimes.
---

# Contract met

Its kind requires two sections and this document writes both, so the section
instance passes.

Its kind also binds a voice regime that forbids a category this engine has no
pattern set for, and a language regime whose profile this engine has no rules
for. Both instances skip, and each skip reason names what it could not read.

## Context

The heading above is the first section the contract requires.

## Decision

The heading above is the second.
