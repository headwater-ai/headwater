---
id: SPEC-FIX-summary-facet-prose
doc_type: design_spec
status: current
status_since: 2026-01-05
title: The organisation doesn't hold a robust reference system.
summary: The organisation doesn't hold this, and the reference system decides.
---

# Summary facet prose

The failing fixture for the front-matter arm of the two lexical rules. Every defect of this document sits in the front matter above, and the body below is clean, so a finding here comes from nowhere else.

The `summary` line carries three defects: a British spelling, a contraction, and a term the regime retired with a replacement. The `title` line carries the same three and no rule reports one. The population is the facet in the `scent` role, and a label in another role is not a sentence.

No finding of this document offers a patch. The two patch shapes that ship reach a run of prose and a relations block, and neither one edits a mapping, which HW-OBL-0103 records.
