---
id: NOTE-FIX-paid
status: discharged
status_since: 2026-08-01
summary: a receipt the corpus paid, at a state only its own regime names
---

# The receipt the corpus paid

`receipt` binds `wide`, which names `discharged`. The state is terminal because
the value carries the role `terminal-retained`, and it is legitimate here
because the regime of this kind names it. A rule reading a list of state names
taken from `narrow` calls this document fine.
