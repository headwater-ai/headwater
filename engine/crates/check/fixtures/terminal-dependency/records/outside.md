---
id: NOTE-FIX-outside
status: discharged
status_since: 2026-08-01
summary: a record standing at a state the regime of its own kind does not name
---

# The record standing outside its own machine

`record` binds `narrow`, and `narrow` names no `discharged`.
`lifecycle.state.not_admitted` reports this document, and this rule stands down
rather than reporting it once more for every document that cites it.
