---
id: NOTE-FIX-mentions-proposed
status: current
status_since: 2026-08-02
relations:
  mentions:
    - NOTE-FIX-proposed
summary: a live record that mentions a record at an initial state its own regime does not name
---

# The live record resting on a document outside its own machine

`proposed` carries the role `initial`, and `narrow` names no such state.
`lifecycle.state.not_admitted` reports the target once, and the initial reading
stands down rather than reporting it once per citation.
