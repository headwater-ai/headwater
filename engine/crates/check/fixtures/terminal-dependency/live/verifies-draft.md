---
id: NOTE-FIX-verifies-draft
status: current
status_since: 2026-08-02
relations:
  verifies:
    - NOTE-FIX-argued-over
summary: a live record that writes the inverse name onto a draft
---

# The live record citing a draft through an inverse name

The relation declares `NOTE-FIX-argued-over verified_by` this document, so the
draft is the declared source. This document wrote the line, so it is the one
that cites, and it rests on a draft.
