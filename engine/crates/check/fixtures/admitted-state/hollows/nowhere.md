---
id: NOTE-FIX-nowhere
status: current
status_since: 2026-08-01
summary: a document whose kind binds a regime that declares no state
---

# The document under a regime that declares nothing

The defect is in the taxonomy and `taxonomy validate` reports it once. A check
that reported it here would report it once per document instead.
