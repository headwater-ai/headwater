---
id: NOTE-FIX-authored-wrong
status: discharged
status_since: 2026-08-01
summary: a record written at a state its own regime does not name
---

# The record authored at the wrong state

It has one version and it makes no movement. Nothing compares it against a
prior, and the state it opens at is a state `narrow` never names.
