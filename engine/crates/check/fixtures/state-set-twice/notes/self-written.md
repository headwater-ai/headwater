---
id: NOTE-FIX-self-written
status: superseded
relations:
  superseded_by:
    - NOTE-FIX-self-superseder
  retired_by:
    - NOTE-FIX-self-retirer
summary: a clash the target wrote itself, through two inverse names
---

# self-written

This file writes `superseded_by` and `retired_by`. Both run toward this
document, so it is the target of `supersedes` and of `retires`, and the clash
is the same one `clash.md` has.
