---
id: NOTE-FIX-inverse
status: superseded
summary: the target of one setter, and the source of another whose far end wrote it
---

# inverse

`inverse-superseder.md` declares `supersedes` here. `retired-by-inverse.md`
writes `retired_by` toward this document, the inverse of `retires`, so the
relation runs from this document to that one. This document sets a state there
and is told none. A rule that read the writing file as the source reports a
clash here.
