---
id: NOTE-FIX-clash
status: superseded
summary: the target of `supersedes` and `retires`, told two states
---

# clash

The decisive case. `clash-superseder.md` declares `supersedes` here and
`clash-retirer.md` declares `retires` here. One writes `superseded` and the
other writes `retired`, and a generated page can show only one of them.
