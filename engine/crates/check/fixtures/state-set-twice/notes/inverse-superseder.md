---
id: NOTE-FIX-inverse-superseder
status: current
relations:
  supersedes:
    - NOTE-FIX-inverse
  cites:
    - NOTE-FIX-inverse
summary: the one setter of `NOTE-FIX-inverse`, which also cites it
---

# inverse-superseder

Declares `supersedes` and `cites` at `NOTE-FIX-inverse`. `cites` writes no
state, so it is never half of a clash.
