287 seen, 207 classified, 207 checked, 3891 check instances
      4 generated, held to regeneration by `headwater generate --check`
     23 skipped: the shelf is heterogeneous, so the discriminator is the gap metadata fills, and kind resolution already read it
      2 skipped: the document at the target end, `HW-REG-open-questions`, declares no value for the state facet, so there is no state to read there
    195 skipped: this document writes no fragment into itself
    415 skipped: change-scoped-only: the prior version is available only in change-scoped evaluation, and this run carries no change
adoption
  0 pairs open, 1 closed, holding 0 findings, in 1 task
  AD-1 0 open, 1 closed, holding 0 findings, owner the specification authors, until 2027-06-30
    The one entry of `.ste-lint-baseline.json` that this corpus still owes. The baseline held two when the linter retired. The other named a semicolon inside a block quotation, which the engine does not report at all, because the parser hands a quoted block to no lexical rule
208 documents in the read set, and 2 barriers that no gate carries across a merge
    207 instances of facet.required.missing
    207 instances of facet.value.not_permitted
    207 instances of identifier.pattern.not_met
    207 instances of shelf.placement_is_primary
    444 instances of relation.target.unresolved
     86 instances of relation.reciprocity.missing
    321 instances of relation.endpoint.not_permitted
      2 instances of lifecycle.dependency.on_terminal
     16 instances of relation.participation.overdue
    207 instances of relation.declaration.unusable
    207 instances of identifier.unusable
      1 instances of identifier.claimed_twice
    187 instances of voice.forbidden_construction
    198 instances of language.controlled.not_met
    198 instances of language.retired_term.used
    198 instances of language.source_form.not_met
    169 instances of section.required.missing
    207 instances of link.fragment.unresolved
    207 instances of warrant.promoted
    207 instances of lifecycle.transition.not_permitted
    207 instances of lifecycle.state.not_admitted
      1 instances of lifecycle.deletion.not_permitted
suppressions
  4 findings hidden by 4 directives
      4 false_positive
      3 of language.controlled.not_met
      1 of language.retired_term.used
      4 on the shelf spec_series
rules, and for each the scope that binds it
  facet.required.missing
    document scope, one document and its front matter
  facet.value.not_permitted
    document scope, one document and its front matter
  identifier.pattern.not_met
    document scope, one document and its front matter
  shelf.placement_is_primary
    document scope, one document and its front matter
  relation.target.unresolved
    edge scope, one relation instance and both endpoints
  relation.target.suspect
    edge scope, one relation instance and both endpoints
  relation.reciprocity.missing
    edge scope, one relation instance and both endpoints
  relation.endpoint.not_permitted
    edge scope, one relation instance and both endpoints
  lifecycle.dependency.on_terminal
    edge scope, one relation instance and both endpoints
  relation.participation.overdue
    neighbourhood scope, one document and the documents one relation away from it, and the injected clock
  relation.declaration.unusable
    document scope, one document and its front matter, and what phase A could not make of it
  identifier.unusable
    document scope, one document and its front matter, and what phase A could not make of it
  identifier.claimed_twice
    corpus scope, every row of the census, and what phase A could not make of each document's identity, and it is a barrier
  voice.forbidden_construction
    document scope, one document, its front matter and its body
  language.controlled.not_met
    document scope, one document, its front matter and its body
  language.retired_term.used
    document scope, one document, its front matter and its body
  language.source_form.not_met
    document scope, one document, its front matter and its body
  section.required.missing
    document scope, one document, its front matter and its body
  link.fragment.unresolved
    document scope, one document, its front matter and its body
  warrant.promoted
    document scope, one document and its front matter, and the version of it that stood before the change
  lifecycle.transition.not_permitted
    document scope, one document and its front matter, and the version of it that stood before the change
  lifecycle.state.not_admitted
    document scope, one document and its front matter
  lifecycle.deletion.not_permitted
    corpus scope, every row of the census, and the version of each path the change named that no row of this corpus holds, and it is a barrier
  coverage.document_unchecked
    corpus scope, every row of the census, and it is a barrier
  obligation.disposition.not_one
    taxonomy scope, the resolved taxonomy, and no document
  control.mechanism.unimplemented
    taxonomy scope, the resolved taxonomy, and no document
register
  31 obligations: 28 verified, 2 gap, 1 unverifiable, 0 with no disposition
     12 high, 11 verified
     14 medium, 12 verified
      5 low, 5 verified
  OB-LANG-1 verified, 3 findings escaped under it
  OB-LANG-3 verified, 1 finding escaped under it
  OB-DR-1 gap, owner the decision-record entry, target a change-scoped rule that reads the prior version, which spec 12 already supplies as `needs_prior`
  OB-DR-2 gap, owner the decision-record entry, target a rule over state dwell time, which `taxonomy audit` already measures and no check reads
  OB-DR-3 unverifiable, To count the decisions in a document is to reason about what the prose asserts, which spec 1 forswears. The shelf is homogeneous and the section contract requires one `Decision` heading, and a second decision under that heading is invisible to both
  28 controls
     25 posture advisory
      3 posture blocking
     28 acts detective
     26 name a rule this engine carries
      2 name a phase this engine runs
     12 with no promotion record, so nothing states what would promote them
      7 with criteria
      3 at a final posture
      4 permanently advisory
      2 producing facts rather than findings
  escaped findings, in the precedence spec 4 fixes: no waiver reaches a check finding, 0 migration-pending, 4 suppressed
  every rule this engine carries reaches one obligation
