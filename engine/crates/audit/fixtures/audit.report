taxonomy audit of audit/fixture 1.0.0
  lock sha256:the-fixture
  at 2026-01-01, over 5 documents and 6 declared edge halves
  5 of them are classified, and 0 generated by this engine and carrying a declared
  identity. Both are nodes, so both are here, and a check report counts the first
  number alone.
  It measures the schema against the corpus, and every finding here is about
  the taxonomy. It gates nothing.
  One bar is declared: `last_verified` is stale after 90 days. It is the only
  number a declaration states, so it is the only reading below that carries a
  finding. Everything else is a distribution, and nothing declares how narrow a
  distribution may get before it is a defect.

findings
  `catalogues` has 1 of 2 halves on a document past the declared window
  `supersedes` has 1 of 2 halves on a document past the declared window

relations, by the creator each one declares
  Q4 keeps `created_by` on the relation type, so every row is a relation and never
  an edge instance. Two halves of one relation carry one value whatever wrote them.
  `captured` is the share of eligible documents that declared at least one half.
  author — 2 relations, 3 of 6 halves
    catalogues         2 halves,   5 eligible,   2 declaring, 40.0% captured
                     1 past the window, 0 on a document with no readable date
    governs              1 half,   5 eligible,   1 declaring, 20.0% captured
                     0 past the window, 0 on a document with no readable date
  scaffold — 1 relation, 2 of 6 halves
    supersedes         2 halves,   5 eligible,   1 declaring, 20.0% captured
                     1 past the window, 0 on a document with no readable date
  generator — no relation declares it
  hook — 1 relation, 0 of 6 halves
    notes              0 halves,   3 eligible,   0 declaring, 0.0% captured
                     0 past the window, 0 on a document with no readable date
  agent — 1 relation, 1 of 6 halves
    annotates            1 half,   1 eligible,   1 declaring, 100.0% captured
                     0 past the window, 1 on a document with no readable date
  import — no relation declares it
  2 of the 6 creators spec 2 declares have no relation in this taxonomy: generator, import.
  A comparison against any of them has one arm over this corpus, and the missing
  arm is a property of the taxonomy rather than of the authoring. No number below
  stands in for one.

relation families
  association  1 relation, 0 halves
  derivation   1 relation, 1 half
    `annotates` declares multinuclear, and the family gives nucleus-satellite
  evidence     1 relation, 2 halves
  governance   1 relation, 1 half
  succession   1 relation, 2 halves
    `supersedes` declares the nuclearity the family already gives

facets, and what each one separates
  status         role state, 4 declared, 5 of 5 documents carry it
                 3 distinct values in use, commonest `current` on 3, 60.0%
  status_since   role state_entered, no declared set, 5 of 5 documents carry it
                 5 distinct values in use, and no two documents share one
  last_verified  role freshness, no declared set, 4 of 5 documents carry it
                 4 distinct values in use, and no two documents share one
  summary        role scent, no declared set, 5 of 5 documents carry it
                 5 distinct values in use, and no two documents share one
  doc_type       no role, 3 declared, 2 of 5 documents carry it
                 2 distinct values in use, and no two documents share one
  tier           no role, 2 declared, 0 of 5 documents carry it
                 no document carries it, so it separates nothing here
  A facet whose documents nearly all share one value separates nearly nothing.
  Nothing declares where that line is, so the share above carries no verdict.
  pairs where one value fixes the other
    `doc_type` fixes `status` over the 2 documents that carry both

shelves that hold several kinds
  The discriminator is self-asserted, and kind decides every obligation after it.
  A shelf that drifts toward its cheapest kind is what this reading shows.
  library — discriminator `doc_type`, 2 documents
        0 catalog
        1 manual
        1 note

dwell in the current state
  Observed and never policed. A state nothing sits in has a row too.
  draft             1 document, 47 to 47 days, median 47
  current          3 documents, 12 to 245 days, median 31
  superseded        1 document, 122 to 122 days, median 122
  deprecated       0 documents, and no dwell reading

warrants, over the closed set spec 3 declares
  A warrant states what stands behind a document, and promotion is the act that
  moves one from `asserted` to `accepted`. The set is closed, so every value has a
  row whether a document of this corpus states it or not.
      1 accepted
      0 regenerated   the engine reads this off the generated-file marker
      0 transcribed   the engine reads this off the generated-file marker
      0 asserted      no document of this corpus states it
  Stated on 2 of 5, and absent on 3. Spec 3 makes the value required, so the
  second figure is a population and never a default. The census reported 0 of these rows
  as generated, and the engine reads the warrant of a generated document off the marker
  rather than out of a declaration.
  Values the closed set does not hold, each with the count that states it. No
  check of this engine reads a warrant, so this is the only line that reports one:
        1 `pending`
  The `asserted` population is 0, which is the denominator a promotion rate divides
  by. Nothing declares how many promotions in one change is too many, so no figure here
  carries a verdict.

what this verb does not measure, and what each one waits on
  Every prerequisite below was evaluated against this corpus on this run, so a
  wait that a corpus has ended says so with no edit to this engine.
  transition continuity — waits
    a record of the state a document left
      nothing declares it — no facet of this taxonomy takes the `state_left` role, and
      spec 2's closed role registry holds no such role, so one entry into a state cannot
      be told from a second
  scent quality — waits
    a cue authored on an edge instance
      nothing authored one — no half of the 6 this corpus declares carries a `cue`
      attribute, which OBL-repo-0023 records
  promotion rate — waits
    an `asserted` population to divide by
      nothing authored one — no document of the 5 classified here states `warrant:
      asserted`, so there is nothing to promote from
    a promotion to count
      designed and unbuilt — a promotion is a lifecycle transition, from the
      `asserted` warrant to `accepted`, and spec 12 already designs the input one needs.
      A check that declares `needs_prior` receives the previously committed version of
      each changed document. No check declares it and nothing implements it. It could
      not be read here in any case, because this verb reads one working tree and the
      prior version is available only in change-scoped evaluation
  3 of 3 still wait. Each one states what ends it: a declaration, an authoring
  pass, or a build of an input this engine designs and does not supply.
