the resolved taxonomy `relations.supersedes.on_target.set_state`: relation coherence: writes `superseded`, and `kinds.receipt` at the `to` end binds the `discharge` lifecycle regime, which never names it. A relation writes a state that the kind it writes onto can stand in
the resolved taxonomy `relations.retires.on_target.set_state`: relation coherence: writes `superseded`, and `kinds.receipt` at the `to` end binds the `discharge` lifecycle regime, which never names it. A relation writes a state that the kind it writes onto can stand in
