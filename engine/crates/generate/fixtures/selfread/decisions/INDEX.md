---
"headwater:generated": "shelf_sections. `headwater generate` writes this file, and `headwater generate --check` holds it. Edit the corpus, not this file."
id: DR-FIX-9999
title: The index of the decisions shelf
status: current
status_since: 2020-01-01
summary: "One heading for each of the 2 documents on the `decisions` shelf, so that a citation naming one of them resolves in this file."
---

# decisions

The committed output of an earlier run, standing in the tree so that the fold has
its own last version available to read. The date above is deliberately older than
every source document, because a value no source supports is the only evidence
that the fold read this file rather than the corpus.

## The stalest source document

[DR-FIX-0001](0001-first.md) — the older of the two sources, so its date is the one a fold of the shelf must answer

## The fresher source document

[DR-FIX-0002](0002-second.md) — the newer of the two sources, present so that the fold has a minimum to take rather than one value
