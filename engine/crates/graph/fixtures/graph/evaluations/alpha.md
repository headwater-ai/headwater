---
id: EVAL-FIX-alpha
relations:
  cited_by: SPEC-FIX-first
---

# The half that is written from the other end

`cited_by` is the inverse of `cites_evidence`, and this document writes it. The edge is the same edge that `00-first.md` declares, seen from its other end, so the two normalize to one triple in the direction the relation declares.
