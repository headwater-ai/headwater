---
id: SPEC-FIX-list-anchor-reordered
doc_type: design_spec
relations:
  governs:
    - [graph.report, graph.taxonomy.yml]
---

# One list written in two orders is one node

The same two files as the first entry of [07-list-anchor.md](07-list-anchor.md),
written in the other order. HW-DR-0074 rules that the identity of an anchor
node is its normalized patterns, sorted, so this is the same node as that
entry, cited by both documents.
