# A file whose name holds a space

A corpus that is not this one holds files named like this, and CommonMark spells the space in a link to one as `%20`. This file is what the escaped destination in `loose.md` means.
