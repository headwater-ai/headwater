# A file whose name holds the escape itself

This name carries the three characters `%`, `2` and `0`, and nothing on a disk decodes them. A binder that decoded a destination before looking would call a link to this file broken, which is why the destination the author wrote is read first.
