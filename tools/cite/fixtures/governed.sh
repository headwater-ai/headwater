#!/bin/sh
# A citation that is true: spec 5 declares a `governs` edge onto this exact
# path, so the document the identifier resolves to is in this file's
# governing set and nothing is reported.
# per HW-SPEC-ai-integration (docs/spec/05-ai-integration.md)
exit 0
